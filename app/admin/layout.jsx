export const metadata = {
  title: "Admin",
  robots: {
    index: false,
    follow: false,
    nocache: true,
    noarchive: true,
  },
};

export default function AdminRootLayout({ children }) {
  return children;
}
